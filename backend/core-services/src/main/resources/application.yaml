spring:
  application:
    name: core-services
  datasource:
    url: jdbc:postgresql://localhost:5432/swift?currentSchema=expenzo_v2
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
server:
  port: 8080
  servlet:
    context-path: /core-services
